# Copy to .env for local development
# cp .env.example .env

# Public (safe to expose to browser) - only if needed
# NEXT_PUBLIC_API_BASE_URL=http://localhost:3000

# Server-only secrets (do NOT commit .env)
# OPENAI_API_KEY=
# SOME_INTERNAL_TOKEN=
