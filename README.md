# **ACE React Demo**

A lightweight front-end demo for **ACE (AI Commerce Exchange)** showcasing the interaction between a user, a mock AI agent, and automated opportunity insights generated by ACE logic.  
This project is designed for rapid prototyping, demos, and future deployment on **AWS (EC2 or ECS Fargate)**.

---

## 🚀 Features

- **Real-time chat UI** simulating interaction between a user and the ACE Agent  
- **ACE Insights panel** updating dynamically based on user input  
- **Scrollable ACE Agent messages** for clean layout and readability  
- **React-based component architecture** for extensibility  
- Deployment-ready structure for **ECS Fargate**, **Docker**, and **CI/CD** automation

---

## 📦 Tech Stack

- **React 18**
- **Vite** (recommended) or Create React App
- **TypeScript** (optional depending on setup)
- **CSS Modules / Tailwind** (if applicable)
- **Docker** for containerized deployment
- **AWS ECS Fargate + Application Load Balancer** for cloud hosting

---

## 📁 Project Structure

